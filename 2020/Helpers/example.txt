#!/usr/bin/env python

def get_desc():
    return DAY_NUM, 'DAY_TODO'

def calc(log, values, mode):
    # from grid import Grid
    # grid = Grid()

    # TODO
    return 0

def test(log):
    values = log.decode_values("""
        TODO
    """)

    log.test(calc(log, values, 1), 1234)
    log.test(calc(log, values, 2), 1234)

def run(log, values):
    log(calc(log, values, 1))
    log(calc(log, values, 2))
